<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>

        <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css" 
        integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">

        <!-- Icons -->
        <script src="https://kit.fontawesome.com/a747dc4c23.js" crossorigin="anonymous"></script>

        <link href="https://fonts.googleapis.com/css?family=Fira+Sans&display=swap" rel="stylesheet">
        {% block stylesheets %}{% endblock %}
    </head>
    <body>
        <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
            <a class="navbar-brand" href="#">Student Management System</a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
                <div class="navbar-nav ml-auto">
                    <a class="nav-item nav-link active" href="{{ path('user_new') }}">Create Student</a>
                    {% if app.user %}
                        <a class="nav-item nav-link active" href="{{ path('user_index') }}">Students</a>
                        <a class="nav-item nav-link active" href="{{ path('teacher_new') }}">Create Teacher</a>
                        <a class="nav-item nav-link active" href="{{ path('teacher_index') }}">Teachers</a>
                        <a class="nav-item nav-link active" href="{{ path('admin_new') }}">Create Admin</a>
                        <a class="nav-item nav-link active" href="{{ path('admin_index') }}">Users</a>
                        <a class="nav-item nav-link active" href="#">{{ app.user.name }}</a>
                        <a class="nav-item nav-link active" href="{{ path('app_logout') }}"><i class="fas fa-sign-out-alt"></i></a>
                    {% endif %}
                </div>
            </div>
	    </nav>
        
        {# {% if app.user %}
            <div class="mb-3"> #}
                {# You are logged in as {{ app.user.roles|json_encode }}, <a href="{{ path('app_logout') }}">Logout</a> #}
                {# You are logged in as {{ app.user.name }}, <a href="{{ path('app_logout') }}">Logout</a>
            </div>
        {% endif %} #}

        {% block body %}{% endblock %}

        <!-- jQuery -->
        <script src="https://code.jquery.com/jquery-3.5.0.min.js"
        integrity="sha256-xNzN2a4ltkB44Mc/Jz3pT4iU1cmeR0FkXs4pru/JxaQ=" crossorigin="anonymous"></script>
        <script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.0/dist/umd/popper.min.js" integrity="sha384-Q6E9RHvbIyZFJoft+2mJbHaEWldlvI9IOYy5n3zV9zzTtmI3UksdQRVvoxMfooAo" crossorigin="anonymous"></script>
        <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/js/bootstrap.min.js" integrity="sha384-wfSDF2E50Y2D1uUdj0O3uMBJnjuUD4Ih7YwaYd1iqfktj0Uod8GCExl3Og8ifwB6" crossorigin="anonymous"></script>
        {% block javascripts %}{% endblock %}

        {# <br>Base Temaplte Updated.............. #}
    </body>
</html>
